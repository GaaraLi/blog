%h1 Tags
%hr
%table
  %thead
    %tr
      %th Name
      %th Delete
  %tbody
    -@tags.each do |tag|
      %tr
        %td
          =tag.name
        %td
          =link_to 'delete', tag, method: :delete, data: { confirm: 'Delete tag ?'}

=link_to 'New Tag', new_tag_path