<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :style=>"height:25px; width:500px" %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, rows:50, cols:150 %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.collection_select :tag_id, Tag.all, "id", "name"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
